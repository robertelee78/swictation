# This directory will contain macOS ARM64 (Apple Silicon) binaries:
# - swictation-daemon (main voice recognition daemon, Mach-O arm64)
# - swictation-ui (Tauri-based UI application, Mach-O arm64)
#
# Binaries are built by CI/CD (GitHub Actions macos-14 runner) or locally via build scripts.
# They are NOT checked into git.
